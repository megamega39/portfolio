こんにちは <%= @resource.email %>さん！

パスワードの再設定をリクエストされました。以下のリンクからパスワードを再設定できます。

<%= "#{root_url}?reset_password_token=#{@token}" %>

このリンクは24時間有効です。

このメールに心当たりがない場合は、このメールを無視してください。
上記のリンクにアクセスしない限り、パスワードは変更されません。
